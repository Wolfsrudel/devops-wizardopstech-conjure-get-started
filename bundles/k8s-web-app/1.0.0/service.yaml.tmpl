apiVersion: v1
kind: Service
metadata:
  name: {{.app_name}}-service
  namespace: {{.namespace}}
  labels:
    app: {{.app_name}}
spec:
  type: ClusterIP
  selector:
    app: {{.app_name}}
  ports:
  - port: 80
    targetPort: {{.container_port}}
    protocol: TCP
    name: http
